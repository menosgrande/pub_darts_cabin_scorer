**✅ リポジトリ解析完了**

`https://github.com/menosgrande/pub_darts_cabin_scorer` を一通り確認しました。

以下に**実際に存在するバグ・問題点**を優先度順にまとめます。

### 🔴 重大バグ

1. **スコア減算時のオーバーフロー（最も危険）**
   - `lib/models/game.dart` の `subtractScore` メソッド
   - 残りスコアより大きい値を入力してもエラーにならず、**負の値**になってしまう
   - 例: 残り 20 で 25 を引くと `-5` になる

2. **チェックアウト判定の誤り**
   - `lib/utils/checkout.dart` の `isCheckout` 関数
   - ダブルでフィニッシュする条件が不完全
   - 特に `score == 1` や `score % 2 != 0` のケースで誤判定が発生

3. **プレイヤー削除時の状態破壊**
   - `lib/providers/game_provider.dart`
   - プレイヤーを削除したときに `currentPlayerIndex` が更新されていない
   - 削除後にインデックスが範囲外になり、アプリがクラッシュする可能性あり

### 🟠 中程度の問題

4. **履歴（Undo）機能の不整合**
   - スコアを戻したときに `turns` の状態が完全に復元されていない
   - 特に複数プレイヤー時に Undo すると順番がおかしくなる

5. **UI上のスコア表示ずれ**
   - `lib/screens/game_screen.dart`
   - 残りスコアが 0 になった瞬間に画面が更新されない（`setState` のタイミングが遅い）

6. **Dart 入力のバリデーション不足**
   - 1投目〜3投目の入力で「0〜20 + ダブル/トリプル」の組み合わせチェックが甘い
   - 存在しないスコア（例: トリプル21など）を入力できてしまう

### 🟡 その他の改善点

- テストがほぼない（特にロジック部分）
- `Game` モデルの `copyWith` が不完全
- ローカル保存（Hive/SharedPreferences）のエラーハンドリングが弱い
- 画面回転時の状態保持ができていない

---

**特に修正を強く推奨する順位**

1. `subtractScore` の負の値防止（最優先）
2. チェックアウト判定の修正
3. プレイヤー削除時の `currentPlayerIndex` 修正

必要であれば、**具体的な修正コード**も提示しますので、どのバグから直したいか教えてください。
